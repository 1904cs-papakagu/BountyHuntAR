/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

// must register appkey 'ViroSample' to use with TestBed app

AppRegistry.registerComponent('ViroSample', () => App);
